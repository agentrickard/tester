services:
  tester.commands:
    class: \Drupal\tester\Commands\TesterCommands
    tags:
      - { name: drush.command }
    arguments: ['@plugin.manager.tester', @config.factory', '@http_client', '@state']
